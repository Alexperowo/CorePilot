# CorePilot — План доведения до идеала (живой документ)

> Идём строго по этапам. Каждый этап: внести → проверить скриптом (`field_test.bat`) →
> прислать отчёт → зафиксировать результат здесь → перейти к следующему.
> Документация, синхронизация `app.py` и финальный ROADMAP — В САМОМ КОНЦЕ (Этап 9),
> когда ядро вылизано. Не раньше, чтобы не переписывать доки дважды.

Легенда статусов: `[ ]` не начато · `[~]` в работе · `[x]` готово и подтверждено отчётом.

---

## ЭТАП 0. Базовый поток (ЯДРО ЖИВО) — ПОДТВЕРЖДЁНО
Уже доказано полевыми отчётами:
- [x] Окружение, версии, секреты (3 провайдера: gemini/groq/openrouter)
- [x] Живая проверка ключей всех 3 облачных провайдеров
- [x] Списки облачных моделей (дедлок устранён)
- [x] Списки локальных моделей (LM Studio + Ollama)
- [x] Конвейер end-to-end (вердикт ОК)
- [x] Демон: задача доходит до `done`
- [x] Cleaner: скан → карантин → восстановление
- [x] Локальные роли не уходят в облако (фикс `_role_mode`)
- [x] Понятные ошибки вместо «not a valid model ID»

**Это фундамент. Дальше — усложнение и расширение охвата.**

---

## ЭТАП 1. Управление памятью (жонглирование моделями)
Цель: убедиться, что при смене ролей модели реально выгружаются (8 ГБ VRAM).
- [ ] Включить в LM Studio «Only Keep Last JIT Loaded Model» + Auto-Evict
- [ ] Включить тумблер «Выгружать модель между агентами» в настройках
- [ ] Расширить `field_test`: новая фаза «VRAM-жонглирование» — последовательно
      дёргает 2 РАЗНЫЕ локальные модели, проверяет, что первая выгрузилась
      (через запрос загруженных моделей у сервера до/после)
- [ ] Отчёт подтверждает: omnicoder выгружается перед qwenclaude

## ЭТАП 2. Конвейер на РЕАЛЬНОМ коде (не «создай файл»)
Цель: проверить настоящую работу с кодом, а не тривиальную задачу.
- [ ] Расширить `field_test`: фаза «Конвейер-код» — в temp-проекте создаётся
      файл с заведомым багом, Конвейеру даётся задача его починить
- [ ] Проверяется: собран контекст, есть план, выданы патчи/диффы, не пустой результат
- [ ] Проверяется песочница: изменения в temp-копии, реальный проект не тронут
- [ ] Отчёт: вердикт + число патчей > 0 + корректный diff

## ЭТАП 3. Демон + Менеджер на ГЛОБАЛЬНОЙ задаче (DAG-разбиение)
Цель: увидеть, как большая цель дробится на под-задачи с зависимостями.
- [ ] Расширить `field_test`: фаза «Менеджер-DAG» — даётся крупная цель,
      проверяется, что `generate_backlog` создал НЕСКОЛЬКО задач с `depends_on`
- [ ] Проверяется порядок: задача с зависимостью не стартует раньше родителя
- [ ] Проверяется заморозка: при провале родителя потомок не выполняется
- [ ] Демон проходит цепочку из 2-3 связанных задач до конца

## ЭТАП 4. Эскалация Оракул→Титан и устойчивость
Цель: проверить «тяжёлую артиллерию» при застревании.
- [ ] Фаза «Эскалация»: смоделировать застревание, убедиться, что Оракул зовётся
- [ ] Проверить потолок попыток (`_MAX_TOTAL_ATTEMPTS`) — не уходит в бесконечность
- [ ] Проверить освобождение памяти перед локальным Титаном (OOM-защита)
- [ ] Отчёт: эскалация сработала, система не зависла

## ЭТАП 5. AI Cleaner — расширенный аудит
Цель: проверить ВСЕ сканеры, не только «глубокий кэш».
- [ ] Фаза «Cleaner-полная»: все 4 скана (кэш, загрузки, дубликаты, автозапуск)
- [ ] Дубликаты: создать 2 одинаковых файла, проверить обнаружение по хэшу
- [ ] Проверить защиту: попытка скана системной папки → отказ
- [ ] Удаление навсегда (после карантина) — отдельная безопасная проверка

## ЭТАП 6. llama.cpp как движок (без LM Studio)
Цель: проверить, что встроенный llama.cpp работает самостоятельно.
- [ ] Установить llama.cpp через `setup.bat` (Vulkan), положить GGUF в `models\`
- [ ] Фаза «llama.cpp»: запустить сервер из UI/менеджера, проверить ответ модели
- [ ] Прогнать Конвейер с ролью на `llamacpp` (порт 8080)
- [ ] Проверить `update_llama.bat`: обновление + backup

## ЭТАП 7. Граничные случаи и устойчивость (СТРЕСС)
Цель: ломать систему намеренно — она должна падать мягко.
- [ ] Фаза «Хаос»: нет сети, битый ключ, недоступный локальный сервер,
      пустая модель, повреждённый файл задачи — каждый случай даёт ПОНЯТНУЮ ошибку
- [ ] Проверить «СТОП ВСЁ» во время работы Конвейера/Демона
- [ ] Проверить параллельные задачи в очереди Демона
- [ ] Длинный прогон (soak): очередь из N задач подряд

## ЭТАП 8. Мобильное приложение-компаньон
Цель: проверить Flet-компаньон (отдельно от десктопа).
- [x] Стаб-проверка структуры (фазы строятся) — в песочнице
- [~] Реальная сборка APK (`flet build apk`) — ⚠️ АКТИВНЫЙ БАГ: лицензии Android SDK (детали в CLAUDE.md)
- [ ] Edge-AI чат с локальной моделью на телефоне
- [ ] (опционально) связь компаньона с десктопом

## ЭТАП 8A. Облачная генерация изображений (бесплатные API) [отклонение, по согласию]
Цель: дать генерацию картинок не только через локальный Forge, но и через бесплатные
облачные API — и сделать всё настраиваемым в UI.
- [x] generate_image: путь через HuggingFace Inference API (FLUX.1-schnell)
- [x] Выбор источника forge/comfy/cloud + фолбэк (локальный недоступен -> облако)
- [x] Поля в UI: источник, Forge URL, ComfyUI URL, модели, облачный провайдер
- [x] Ротация ключей для облачного image-провайдера (перебор API_KEYS)
- [x] ComfyUI как второй локальный источник (workflow API)
- [ ] ПРОВЕРКА: реальная генерация (Forge/ComfyUI запущены ИЛИ HF-ключ)

## ЭТАП 8B. Мобильное приложение -> автономный чат-бот [отклонение, по согласию]
Цель: помимо Edge-AI (локальные модели), дать в приложении облачные API как чат-бот
с ротацией ключей и генерацией картинок.
- [x] CloudAPIManager: round-robin до 4 ключей; 429/401/403 -> следующий ключ;
      max len(keys) попыток; «все ключи исчерпаны» -> понятная ошибка (cloud_api.py)
- [x] Настройки: ввод ключей провайдеров (HF/OpenRouter/Cerebras/Groq/OpenAI), до 4 через запятую
- [x] Чат: Dropdown выбора цели на лету [Local]/[PC]/[Cloud: X]
- [x] Картинки в чате: _add_bubble -> ft.Image (base64)
- [x] Генерация картинок с телефона (HF Inference API, FLUX/SDXL)
- [x] Сеть в page.run_thread() (не блокирует UI Flet)
- [x] llama_server/android_bridge не тронуты; cloud_api.py кроссплатформенный
- [ ] ПРОВЕРКА на реальном устройстве (сборка APK + ключи + текст/картинка)

> После 8A/8B ВОЗВРАЩАЕМСЯ на основной путь (обещание зафиксировано).

## ЭТАП 9. ФИНАЛ — Документация и синхронизация (ТОЛЬКО ПОСЛЕ 0-8)
Цель: привести ВСЁ описание в соответствие реальному коду.
- [ ] README.md, INSTALL.md — финальная вычитка (частично сделано)
- [ ] CONFIGURATION.md — все актуальные настройки/тумблеры
- [ ] USAGE.md — все режимы и вкладки в текущем виде
- [ ] TROUBLESHOOTING.md — все реальные проблемы и решения из полевых тестов
- [ ] ROADMAP.md — обновить план развития
- [ ] Мобильное приложение описано во всех релевантных доках
- [ ] Комментарии в коде — вычитать на устаревшее (Chainlit-эпоха и т.п.)
- [ ] `app.py` — синхронизировать с ядром, насколько возможно (запасной веб-UI)
- [ ] Финальный полный прогон `field_test.bat` — всё зелёное

---

## Журнал прогресса
> Сюда вписываем итог каждого этапа: номер итерации архива, что подтвердил отчёт.

- Этап 0: ПОДТВЕРЖДЁН (iter10-11, отчёты показали Конвейер ОК, Демон done, Cleaner ОК).
- Этап 1: _ожидает полевого теста_ (ротация лога — СДЕЛАНО в iter24; readiness-probe выгрузки VRAM — СДЕЛАНО в iter24; тумблер «Выгружать между агентами» — СДЕЛАНО; нужна живая проверка VRAM-жонглирования с 2 разными моделями).
- Этап 8A (отклонение): КОД ГОТОВ (iter24). Генерация через Forge/ComfyUI/HF в tools.py, настройки в UI. Ожидает проверки на реальном Forge или HF-ключе.
- Этап 8B (отклонение): КОД ГОТОВ (iter24-30). cloud_api.py, настройки в UI, crash logger, client_storage. Ожидает сборки APK (⚠️ баг лицензий Android SDK) и теста на устройстве.
- iter24: добавлены peek_api_key, межпровайдерный fallback, RotatingFileHandler, thread-safety BackgroundRunner.
- iter29: профили перенесены в user_data/, демон фильтрует системные файлы, атомарная запись, экспорт/импорт профилей. llama_manager: --no-mmap, контекст 4096 дефолт.
- iter30: мобильное — настройки в client_storage, crash logger, build_apk.bat переписан (авто-лицензии, поиск SDK). APK всё ещё не собирается из-за лицензий Android SDK.
