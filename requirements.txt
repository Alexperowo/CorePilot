# CorePilot (AI Factory) — зависимости
# Python 3.12.x (Windows 10/11)
#
# Версии сверены на июнь 2026. При обновлении CrewAI/DearPyGui сверяйтесь с их
# changelog — у библиотек бывают ломающие изменения API.

# --- Основной графический интерфейс ---
dearpygui>=2.0.0,<3.0.0

# --- Ядро фабрики агентов ---
crewai==1.14.6
# Gemini у CrewAI — НАТИВНЫЙ провайдер (нужен отдельный SDK-пакет). Без него
# роли на Gemini падают: "Google Gen AI native provider not available".
# Если Gemini не используешь — эту строку можно убрать.
crewai[google-genai]==1.14.6
# litellm: CrewAI сам тянет совместимую версию (в его changelog бывают
# принудительные даунгрейды litellm под совместимость). Поэтому НЕ пиним жёстко
# вразрез с CrewAI, но ЯВНО исключаем скомпрометированные сборки 1.82.7/1.82.8
# (supply-chain инцидент марта 2026, кредентел-стилер). Берём проверенную линию.
litellm>=1.86.0,!=1.82.7,!=1.82.8
pydantic>=2.7.0,<3.0.0
psutil>=5.9.0
requests>=2.31.0

# --- Запасной веб-интерфейс (необязателен; основной UI — DearPyGui) ---
# Раскомментируйте, если хотите запускать старый Chainlit-интерфейс (app.py).
# ВНИМАНИЕ: Chainlit исторически конфликтует с новыми Pydantic — при включении
# проверьте совместимость версий.
# chainlit==2.11.1

# tomllib — часть стандартной библиотеки Python 3.11+, отдельная установка не нужна.

# --- Для разработки/тестов (необязательно для запуска приложения) ---
# Юнит-тесты ядра: `pytest tests/`  (или без pytest: `python tests/run_all.py`)
# pytest>=8.0.0
