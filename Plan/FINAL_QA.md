# FINAL_QA.md — Финальная проверка перед стабильной бетой

## Автоматические тесты (можно без живой системы)
- [x] `python tests/run_all.py` → 37/37 пройдено
- [x] `python auto_qa.py` → VERD: PASS (хаос-тест демона)
- [ ] `python diagnose.py` → проверить окружение (запускать перед полевым тестом)

## Полевой тест (нужны ключи провайдеров)
- [ ] `python field_test.py` — 10 фаз:
  - Фаза 1: проверка ключей (gemini, openrouter, groq)
  - Фаза 2: листинг облачных моделей
  - Фаза 3: листинг локальных моделей
  - Фаза 4: AI Cleaner — scan
  - Фаза 5: AI Cleaner — карантин
  - Фаза 6: AI Cleaner — восстановление
  - Фаза 7: AI Cleaner — удаление навсегда
  - Фаза 8: Конвейер — gatherer
  - Фаза 9: Конвейер — полный прогон
  - Фаза 10: Демон — добавить/выполнить задачу

## Десктопный UI — все вкладки (нужен запуск `python ui_dpg.py`)
- [ ] Настройки: профили (создать, переключить, удалить) — combo не зависает
- [ ] Настройки: ключи API — маскируются в UI, не выводятся в лог
- [ ] Llama-сервер: запуск/остановка, тумблер LAN, определение VRAM
- [ ] Конвейер: полный прогон (gatherer → architect → coder → auditor)
- [ ] Демон: добавить задачи, запустить, остановить, DAG-зависимости
- [ ] AI Cleaner: scan → quarantine → undo → permanent delete
- [ ] Лог: отображается в реальном времени
- [ ] QA-вкладка: авто-тест из UI

## Мобильное (нужен эмулятор или устройство)
- [ ] APK запускается без серого экрана и без краша при старте
- [ ] Настройки: ввод ключей, системный промпт, temperature/max_tokens
- [ ] Чат Cloud: сообщение отправляется, ответ приходит
- [ ] Чат PC: подключение к десктопу через LAN
- [ ] Edge AI: права на хранилище → список моделей → чат с локальной моделью
- [ ] История чата сохраняется при переключении вкладок (не «амнезия»)
- [ ] Картинки в чате: если ответ содержит URL/base64 изображения — отображается

## Проверка безопасности
- [ ] `secrets.toml` НЕ попадает в `git status` и `git log`
- [ ] `user_data/` НЕ попадает в git
- [ ] Ключи не выводятся в лог демона (`auto_tasks/daemon.log`)
- [ ] `python -c "import agents; agents.peek_api_key('gemini')"` → возвращает маску, не сам ключ

## Критерий «стабильная бета»
Все пункты выше зелёные + нет крашей при штатном использовании в течение одного дня.
